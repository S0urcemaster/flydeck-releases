# Parked Cron wheel

`CronWheelDialer.tsx` and its CSS module preserve the previous circular,
two-ring CRON experiment with the four corner buttons `SCALE`, `SEND`, `RANGE`,
and `ZOOM`. It is intentionally not exported or rendered while the vertical
timeline prototype is evaluated.
