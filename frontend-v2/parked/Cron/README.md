# Archived CRON module

The V2 `CronModule` and `CronDialer` were removed from the active application,
navigation, component catalog, and component lab before the beta release.

Their implementation and focused tests are retained here as reference only.
Nothing below this directory is part of the V2 TypeScript build.
