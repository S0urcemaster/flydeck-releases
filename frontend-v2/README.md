# Flydeck Frontend V2

Independent rebuild of the Flydeck frontend. The current frontend remains
available in `../frontend`.

```bash
npm install
npm run dev --workspace flydeck-frontend-v2
```

The architectural proposal lives in [`rebuild-plan.md`](./rebuild-plan.md).

